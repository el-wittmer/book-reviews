<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/style.css">

   <title>Book Journal Portfolio Project</title>
  </head>
  <body>
    <main>
      <%- include('header.ejs')%>
      <%- include('header_wave.ejs') %>
      <div class="album pt-5 bg-body-tertiary pb-4">
        <div class="container">
          <div class="row">
            <% for (let i = 0; i < locals.data.length; i++){%>
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
              <div class="card shadow-sm my-2">
                    <% let imgUrl = "/images/" + locals.data[i].isbn + '.png'; %>
                    <img src=<%=imgUrl%>>
                    <div class="card-body">
                        <p class="card-text">
                            <%=locals.data[i].title%>
                            <br />
                            <%=locals.data[i].author%>
                            <br />
                            <% if (locals.data[i].year != null) {%>
                                <small class="text-body-secondary">Finished <%=locals.data[i].month%> <%=locals.data[i].year%></small>
                            <%};%>
                            <%const star = "★"%>
                            <p>Rating: <%=star.repeat(locals.data[i].rating)%></p>
                        </p></p>
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group">
                            <a href="/view?id=<%=locals.data[i].id%>"><button
                                type="button"
                                class="btn btn-sm btn-outline-dark mr-1"
                            > View </button></a>
                            <a href="/edit?id=<%=locals.data[i].id%>"><button
                                type="button"
                                class="btn btn-sm btn-outline-dark mr-1"
                            > Edit </button></a>
                            <!-- <a href="/delete?id=<%=locals.data[i].id%>"><button
                                type="button"
                                class="btn btn-sm btn-outline-dark mr-1"
                            > Delete </button></a> -->
                        </div>
                  </div>
                </div>
              </div>
            </div>
            <%};%>
          </div>
        </div>
      </div>
    </main> 
    <%- include('footer.ejs'); %>
    <%- include('script.ejs'); %>
  </body>
</html>
